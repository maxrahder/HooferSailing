When defining a class, the "extend" config can be:
a. The string name of the parent class
b. An array of strings of all parent classes
c. A list of class members being overridden
d. True or false, indicating whether the class may be extended
e. None of the above

When defining a class, the "override" config is:
a. Synonymous with the "extend" config
b. The name of the class whose members are being overridden
c. True or false, indicating whether the class may be extended
d. None of the above

Using the following class definition, what is the proper way to change the weight of "myMammal"?
Ext.define('MyApp.mammal.Mammal', {
	config: {
		weight: 0
	}
});
var myMammal = Ext.create('MyApp.mammal.Mammal');

a. myMammal.set('weight', 100);
b. myMammal.setWeight(100);
c. myMammal.updateWeight(100);
d. myMammal.applyWeight(100);
e. myMammal.weight = 100;

Using the following class definition, what is the proper way to make sure the mammal's weight can only be greater than or equal to zero? 
Ext.define('MyApp.mammal.Mammal', {
	config: {
		weight: 0
	}
});

a. Add this method to the class:
	setWeight: function(weight){
    	if (weight >= 0){
    		this.weight = weight;
	    }
	}
b. Add this method to the class:
	applyWeight: function(weight){
    	if (weight >= 0){
    		return weight;
	    }
	}
c. Add this method to the class:
	updateWeight: function(weight){
    	if (weight >= 0){
    		this.weight = weight;
	    }
	}
d. Modify the config as follows:
	config: {
		weight: Ext.Number.constrain(weight, 0, Number.MAX_VALUE)
	}


Using the following class definition, what is the proper way to reference the phylum of myMammal? 
Ext.define('MyApp.mammal.Mammal', {
    statics: {
    	phylum: 'chordata'
    }
});
var myMammal = Ext.create('MyApp.mammal.Mammal');

a. myMammal.phylum
b. myMammal.getPhylum()
c. MyApp.mammal.Mammal.phylum
d. MyApp.mammal.Mammal.getPhylum()
e. (a) or (c)
f. (b) or (d)



